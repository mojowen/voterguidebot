.mui-row
  .mui-col-md-3
  .mui-col-md-6
    %h2 Select UsersUsers
    %p
      %em
        Select a user and click
        %strong.mui--text-accent Promote
        to create an admin.
    %p
      %em
        Select a user and click
        %strong.mui--text-default
          %a
            %i.fa.fa-user-secret
            Impersonate
        to log in as if you are them.
    %br
    - User.all.each do |user|
      = render layout: 'layouts/user', locals: { user: user } do
        %p
          - if user.admin?
            %strong.mui--text-accent Admin
          - else
            = button_link_to 'Promote', admin_promote_path(user: user.id), method: :post

        %p
          = link_to admin_impersonate_path(user: user.id), method: :post do
            %i.fa.fa-user-secret
            Impersonate

