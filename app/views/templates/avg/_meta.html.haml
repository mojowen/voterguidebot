%title= (content_for(:title) ?  "#{yield(:title).strip} :: The American Voter Guide" : "The American Voter Guide")

%meta{ property:"og:title", content: content_for(:title) ? "#{yield(:title).strip} :: The American Voter Guide" : "The American Voter Guide" }
%meta{ property:"og:url", content: "https://#{ENV['AVG_BUCKET'] || 'preview.americanvoterguide.org'}/#{yield(:url).strip}" }
%meta{ property:"og:description", content: yield(:meta_description).strip }
%meta{ property:"og:image", content: yield(:meta_image).strip }
%meta{ property:"og:type", content:"website" }

%meta{ name:"twitter:card", content:"summary_large_image" }
%meta{ name:"twitter:site", content:"@whywevote2016" }
%meta{ name:"twitter:creator", content:"@whywevote2016" }
%meta{ name:"twitter:title", content: content_for(:title) ? "#{yield(:title).strip} :: The American Voter Guide" : "The American Voter Guide" }
%meta{ name:"twitter:description", content: yield(:meta_description).strip }
%meta{ name:"twitter:image", content: yield(:meta_image).strip }

%meta{ name:"Description", content: content_for(:meta_description) ? yield(:meta_description).strip : "Your one stop shop for all you need for this election like what's on your ballot and where candidates, nationally and locally, stand on the issues" }


:javascript
  var related = 'whywevote2016'
  var election_date = #{ (content_for(:election_date) ? yield(:election_date).strip : "Nov. 16th").to_json }
  var defaultTags = 'voting,election,whywevote'
  var defaultMessage = "Election Day is "+election_date+". Here's everything you need to vote, incl where people running for office stand on issues"
  var hashtag = 'whywevote'
