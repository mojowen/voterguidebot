describe('LooukupComponent', function() {

  beforeEach(function() {
    this.setUpComponent(LooukupComponent, {})
  })

  xit('it renders successfully', function() {
    expect(this.dom.className.search('.dz-clickable')).not.toEqual(-1)
  })

  xit('handles a valid response', function() {
  })

  xit('handles an invalid response', function() {
  })
})
