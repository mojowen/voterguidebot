describe('Question', function() {
  beforeEach(function() {
    this.props = {}
    this.setUpComponent(Question, this.props)
  })

  it('initializes new question')
  it('can be edited')
  it('has answers for every candidate')
})

